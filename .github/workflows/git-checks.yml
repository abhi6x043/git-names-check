name: Testing Git checks

on: push

jobs:
  git-checks:
    runs-on: ubuntu-latest
    timeout-minutes: 10
    steps:
    - name: Checkout code
      uses: actions/checkout@v3

    - name: check commits
      run: |
        #COMMIT_MSGS = $(git log --pretty=format:"%s" ${{ github.event.after }}..${{ github.event.before }})
        COMMIT_MESSAGES==$(git log --format=%B ${{ github.sha }})
        for commit in $COMMIT_MESSAGES; do
          echo $commit
        done